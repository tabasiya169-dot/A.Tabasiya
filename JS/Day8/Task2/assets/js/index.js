

const printStudentNames = () => {
    let studentNames = ["Arun", "Priya", "Kavin", "Divya", "Rahul"];

    for (let i = 0; i < studentNames.length; i++) {
        console.log(studentNames[i]);
    }
};

printStudentNames();